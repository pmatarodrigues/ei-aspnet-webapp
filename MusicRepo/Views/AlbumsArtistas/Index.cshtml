@model IEnumerable<MusicRepo.Models.AlbumsArtista>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Ano)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Album.Nome)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Artista.Nome)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Ano)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Album.Nome)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Artista.Nome)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.idAlbumsArtista }) |
            @Html.ActionLink("Details", "Details", new { id=item.idAlbumsArtista }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.idAlbumsArtista })
        </td>
    </tr>
}

</table>

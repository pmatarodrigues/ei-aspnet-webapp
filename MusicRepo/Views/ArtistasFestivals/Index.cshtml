@model IEnumerable<MusicRepo.Models.ArtistasFestival>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Artista.Nome)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Festival.Nome)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Artista.Nome)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Festival.Nome)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.idArtistasFestival }) |
            @Html.ActionLink("Details", "Details", new { id=item.idArtistasFestival }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.idArtistasFestival })
        </td>
    </tr>
}

</table>

@model IEnumerable<MusicRepo.Models.Album>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Nome)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Ano)
        </th>
        <th>
            @Html.Label("Artista")
            <!--
            @Html.DisplayNameFor(model => model.Artista1.Nome)
                -->
        </th>
        <th>
            @Html.Label("Editora")
            <!--
            @Html.DisplayNameFor(model => model.Editora1.Nome)
                -->
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Nome)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Ano)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Artista1.Nome)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Editora1.Nome)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.idAlbum }) |
            @Html.ActionLink("Details", "Details", new { id=item.idAlbum }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.idAlbum })
        </td>
    </tr>
}

</table>

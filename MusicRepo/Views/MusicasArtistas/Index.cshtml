@model IEnumerable<MusicRepo.Models.MusicasArtista>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Artista.Nome)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Musica.Nome)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Artista.Nome)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Musica.Nome)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.idMusicasArtista }) |
            @Html.ActionLink("Details", "Details", new { id=item.idMusicasArtista }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.idMusicasArtista })
        </td>
    </tr>
}

</table>

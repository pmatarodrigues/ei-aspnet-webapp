@model IEnumerable<MusicRepo.Models.AlbumsEditora>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Ano)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Album.Nome)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Editora.Nome)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Ano)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Album.Nome)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Editora.Nome)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.idAlbumsEditora }) |
            @Html.ActionLink("Details", "Details", new { id=item.idAlbumsEditora }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.idAlbumsEditora })
        </td>
    </tr>
}

</table>
